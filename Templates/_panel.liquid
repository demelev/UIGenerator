using UnityEngine;
using UnityEngine.UI;
using System.Collections;
using System.Collections.Generic;
using System;

namespace {{config.ProjectNamespace}}
{
    public class {{panel.class_name}} : PanelBase
    {
    #region Public events
        {% comment %}
                    }{% for event in panel.PublicEvents %}
                    }    {{event.ToString()}}
                    }{% endfor %}
                    {% endcomment %}
    #endregion


    #region Private serializable variables

        {% if panel.buttons -%}
        {% for element in panel.buttons -%}

        [SerializeField]
        Button {{ element | private_member: "Button" }};
        {% endfor -%}
        {%endif-%}

    #endregion


    #region Public methods and properties

    #endregion


#region {{panel.class_name}} methods

        {% if panel.buttons -%}
            {% for element in panel.buttons -%}
        void {{ element | button_handler }}()
        {
        }
            {% endfor -%}
        {%endif-%}

#endregion

#region Monobehavior methods

        void Awake()
        {

        {% if panel.buttons -%}
        {% for element in panel.buttons -%}
        {{ element | private_member: "Button" }}.onClick.AddListener({{ element | button_handler }});
        {% endfor -%}
        {%endif-%}

        }

        void OnEnable()
        {
        }

        void OnDisable()
        {
        }
#endregion
    }
}
